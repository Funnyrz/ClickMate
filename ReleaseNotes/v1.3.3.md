优化主界面性能，改善快速滚动和页面切换时的流畅度。

- 将后台助手心跳快照读取移出主线程，避免周期性阻塞界面。
- 心跳内容未变化时不再触发无效的界面刷新和权限状态同步。
- 优化菜单布局与应用列表的视图构建开销。
- Release 构建明确禁用代码覆盖率插桩，并在打包阶段增加校验。

安装后如果 macOS 阻止运行，请执行：

```shell
xattr -dr com.apple.quarantine /Applications/ClickMate.app
```
